你需要帮我完成一个轻量级的右手骨架 UDP 传输与 Windows 渲染项目。

背景：
我有一个 Linux 端遥操作程序，会从 Vision Pro / ROS 节点里读取右手手部骨骼数据。Windows 接收端性能较差，不能安装 Unity，也不希望用重型 3D 引擎。我只需要一个轻量的右手骨架可视化窗口，用于辅助观察，不参与遥操作控制闭环。

目标：
做一个简单、稳定、容易排查问题的项目。Windows 端只负责接收 UDP 数据并渲染右手骨架。Linux 端可以先用 dummy 数据测试，后续再接 ROS/VisionPro 数据。

核心设计：
1. Windows 渲染器只认一个中间格式：
   right_hand_21x3 float32
   shape = (21, 3)

2. 点序使用 MediaPipe 风格：
   0: wrist
   1-4: thumb
   5-8: index
   9-12: middle
   13-16: ring
   17-20: pinky

3. Windows 端不直接依赖 Vision Pro、ROS 或 MediaPipe。
   它只接收 UDP 包，解析 21×3 float32，然后画出来。

4. Linux/ROS/VisionPro 端后续只需要实现：
   convert_to_21x3(msg) -> np.ndarray shape=(21,3), dtype=float32

项目语言和库：
- Python 3
- Windows 接收端使用 pygame + numpy
- 发送端使用 socket + struct + numpy
- 不使用 Unity
- 不使用 Unreal
- 不使用 heavy 3D engine
- 不使用深度学习模型
- 不依赖摄像头检测

建议项目结构：
hand_udp_viewer/
├── README.md
├── requirements.txt
├── protocol.py
├── send_dummy_hand.py
├── hand_viewer_pygame.py
└── send_ros_hand_stub.py

其中：
1. protocol.py
   - 定义统一 UDP 包头
   - 提供 pack_right_hand_packet()
   - 提供 unpack_right_hand_packet()
   - 包头需要包含：
     magic: b"VTLP"
     version: uint8
     msg_type: uint8
     frame_id: uint32
     timestamp: float64
     n_floats: uint16

   推荐 struct 格式：
   HEADER_FMT = "<4s B B I d H"

   msg_type:
   MSG_HAND_RIGHT = 11

   payload:
   21 * 3 个 float32

2. send_dummy_hand.py
   - 生成一个假的右手 21 点数据
   - 手指有轻微动态摆动
   - 通过 UDP 发给 Windows 接收端
   - 支持命令行参数：
     --target-ip
     --port
     --rate

   示例：
   python send_dummy_hand.py --target-ip 192.168.1.50 --port 5005 --rate 30

3. hand_viewer_pygame.py
   - Windows 上运行
   - UDP 接收 right_hand_21x3 数据
   - 使用 pygame 渲染右手骨架
   - 不要画球关节
   - 视觉风格尽量做成轻量“科技感骨架”：
     - 深色背景
     - 青蓝色骨条
     - 关节用小环或六边形节点
     - 指尖稍亮
     - 掌心用半透明多边形/线框
     - 可以有非常轻量的 glow 效果，但不能太吃性能
   - 支持命令行参数：
     --listen-ip 默认 0.0.0.0
     --port 默认 5005
     --width 默认 960
     --height 默认 720
     --fps 默认 30
     --scale 默认 220
     --flip-x
     --flip-y
     --axis xy/xz/yz
     --smooth 默认 0.0，可以简单做 EMA 平滑
     --low-power，开启后关闭 glow 和背景网格

   渲染要求：
   - 只渲染最新帧，不排队播放旧帧
   - 如果超过 timeout 没收到数据，显示 Waiting for right hand UDP data...
   - 左上角显示：
     frame_id
     recv fps
     latency_ms
     packet rate
   - 如果数据轴向不对，可以通过 --axis、--flip-x、--flip-y 调整，而不是改代码

4. send_ros_hand_stub.py
   - 先写成 stub，不强依赖 ROS 安装
   - 里面提供函数：
     convert_to_21x3(msg)
   - 用注释说明后续把 ROS topic 数据转换为 21×3 float32 后，调用 protocol.pack_right_hand_packet() 发送
   - 不要让 ROS 发送阻塞遥操作主程序
   - 建议发送端独立进程运行

手部连接关系：
HAND_BONES = [
    (0,1), (1,2), (2,3), (3,4),
    (0,5), (5,6), (6,7), (7,8),
    (0,9), (9,10), (10,11), (11,12),
    (0,13), (13,14), (14,15), (15,16),
    (0,17), (17,18), (18,19), (19,20),
    (5,9), (9,13), (13,17), (5,17),
]

视觉要求：
- 不要使用球形关节
- 关节画成小环或六边形节点
- 骨头画成粗一点的发光线/骨条
- 背景深色
- 整体轻量，适合性能很差的 Windows 电脑
- low-power 模式下尽量只画普通线条和小环

性能要求：
- Windows 渲染端 30 FPS 即可
- 可以降到 20 FPS
- 不要无限制渲染
- 不要使用复杂 shader
- 不要做 3D mesh
- 不要做阴影
- 不要做后处理
- 不要依赖 GPU 强性能

排查要求：
README.md 里必须写清楚排查步骤：

1. Windows 端安装依赖：
   pip install -r requirements.txt

2. Windows 运行接收端：
   python hand_viewer_pygame.py --port 5005

3. Linux 或另一台电脑运行 dummy 发送端：
   python send_dummy_hand.py --target-ip <Windows_IP> --port 5005 --rate 30

4. 如果看不到：
   - 检查 Windows IP
   - 检查端口是否一致
   - 检查 Windows 防火墙是否允许 Python UDP
   - 尝试在同一台机器上用 127.0.0.1 测试
   - 尝试 --low-power
   - 尝试 --fps 20 --width 640 --height 480
   - 尝试 --flip-x / --flip-y / --axis xz

5. 如果手方向不对：
   - 用 --axis xy/xz/yz 切换投影平面
   - 用 --flip-x / --flip-y 翻转
   - 用 --scale 调整大小

6. 如果画面抖动：
   - 使用 --smooth 0.3 或 --smooth 0.5
   - 但不要让平滑造成明显延迟

请生成完整代码，保证能直接运行 dummy sender + pygame receiver。
优先保证稳定、简单、可排查，不追求高级视觉效果。


先只实现 dummy sender + pygame receiver，不要先接 ROS。保证我能在没有 Vision Pro 和没有 ROS 的情况下看到一只动态右手骨架。